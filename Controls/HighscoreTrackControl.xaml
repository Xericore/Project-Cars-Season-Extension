<UserControl x:Class="ProjectCarsSeasonExtension.Controls.HighscoreTrackControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" Name="TrackControl">
    <Grid>
        <GroupBox Header="{Binding ElementName=TrackControl, Path=Title}">
            <StackPanel>
                <DockPanel Height="150" Margin="10,10,10,10">
                    <Image Source="/Assets/Track.png" Name="TrackImage" DockPanel.Dock="Left" />
                    <TextBlock Width="400" Name="Description" />
                    <Image Source="/Assets/Car.png" Name="CarImage" DockPanel.Dock="Right" />
                </DockPanel>
                <DataGrid ItemsSource="{Binding ElementName=TrackControl, Path=Player}" ColumnWidth="*" RowHeight="25"
                          CanUserAddRows="False"
                          IsReadOnly="True" />
            </StackPanel>
        </GroupBox>
    </Grid>
</UserControl>