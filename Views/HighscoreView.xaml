<Page x:Class="ProjectCarsSeasonExtension.Views.HighscoreView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ProjectCarsSeasonExtension.Views"
      Title="HighscoreView">
    <Grid Margin="10">
        <GroupBox Header="Track Name">
            <StackPanel>
                <DockPanel Height="150" Margin="10,10,10,10">
                    <Image Source="/Assets/Track.png" Name="TrackImage" DockPanel.Dock="Left"/>
                    <TextBlock Width="400" Name="Description"/>
                    <Image Source="/Assets/Car.png" Name="CarImage" DockPanel.Dock="Right"/>
                </DockPanel>
                <DataGrid ItemsSource="{Binding Player}" ColumnWidth="*" RowHeight="25" CanUserAddRows="False"
                          IsReadOnly="True">

                </DataGrid>
            </StackPanel>
        </GroupBox>
    </Grid>
</Page>
