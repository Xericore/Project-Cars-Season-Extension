<Page x:Class="ProjectCarsSeasonExtension.Views.ChallengeView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ProjectCarsSeasonExtension.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ChallengeView" DataContext="{Binding RelativeSource={RelativeSource Self}}">

    <DataGrid Name="ChallengeDataGrid" ItemsSource="{Binding ChampionshipView.ChallengeStandings}"
              CanUserAddRows="False" AutoGenerateColumns="False"
              IsReadOnly="True">
        <DataGrid.Columns>
            <DataGridTextColumn Header="Player" Binding="{Binding Player.Name}" />
        </DataGrid.Columns>
        
    </DataGrid>
</Page>
