<Page x:Class="ProjectCarsSeasonExtension.Views.SeasonView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ProjectCarsSeasonExtension.Views"
      xmlns:converters="clr-namespace:ProjectCarsSeasonExtension.Converters"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      
      Background="{StaticResource MainBackgroundColor}"
      TextElement.Foreground="{DynamicResource MaterialDesignBody}"
      TextElement.FontWeight="{StaticResource MainFontWeight}"
      TextElement.FontSize="{StaticResource MainFontSize}"
      FontFamily="{DynamicResource MaterialDesignFont}"

      Title="SeasonView" DataContext="{Binding RelativeSource={RelativeSource Self}}">

    <Grid Margin="0,10,0,0">

        <TabControl Name="ChallengesTabControl" HorizontalAlignment="Center" BorderThickness="0" ItemsSource="{Binding ChallengeStandings}">

            <TabControl.Resources>
                <converters:TrackNameToImageSourceConverter x:Key="TrackNameToImageSourceConverter" />
            </TabControl.Resources>

            <TabControl.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="5">
                        <Grid.Resources>
                            <Style TargetType="TextBlock">
                                <Setter Property="TextWrapping" Value="Wrap"></Setter>
                            </Style>
                        </Grid.Resources>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition />
                        </Grid.ColumnDefinitions>

                        <Image Grid.Column="0"
                               Source="{Binding Challenge.TrackName, Converter={StaticResource TrackNameToImageSourceConverter}}"
                               Height="96" Width="96"/>

                        <Grid Grid.Column="1">
                            <Grid.RowDefinitions>
                                <RowDefinition />
                                <RowDefinition />
                                <RowDefinition />
                            </Grid.RowDefinitions>

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition />
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Track:"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Challenge.TrackName}"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Car:"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Challenge.CarName}"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Difficulty:"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Challenge.Difficulty}"/>

                        </Grid>
                    </Grid>
                </DataTemplate>
            </TabControl.ItemTemplate>

            <TabControl.ContentTemplate>
                <DataTemplate>
                    <Frame Content="{Binding ChallengeView}" HorizontalAlignment="Center" NavigationUIVisibility="Hidden" />
                </DataTemplate>
            </TabControl.ContentTemplate>

        </TabControl>
    </Grid>
</Page>
